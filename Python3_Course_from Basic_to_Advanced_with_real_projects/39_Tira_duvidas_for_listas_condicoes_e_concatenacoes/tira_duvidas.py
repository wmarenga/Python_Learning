"""
secreto = 'python'
secreto_temporario = ''

for letra_secreta in secreto:
    if letra_secreta in digitadas:
        secreto_temporario += '-*-'

# Pular a letra 't'

secreto = 'python'
secreto_temporario = ''

for letra_secreta in secreto:
    if letra_secreta == 't':
        pass
    else:
        print(letra_secreta)
"""
