from modulo import soma

print(soma(10, 30))
# print(__name__)

# Se o módulo for importado diretamente, ele se chamará __main__ e se for outro
# módulo se chamará o nome do módulo mesmo (modulo) neste exemplo.
