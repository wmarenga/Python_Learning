"""
Exercicio 72

Considerando a estrutura:

struct Vetor{
    float x;
    float y;
    float Z;
};

para representar um vetor no R**3, implemente uma funcao que calcule a soma de dois vetores. 
Essa funcao deve obedecer ao prototipo:

void soma (struct Vetor * v1, struct Vetor * v2, struct Vetor * res);

onde os parametros v1 e v2 sao ponteiros para os vetores a serem somados, e o parametro res
e um ponteiro para uma estrutura vetor onde o resultado da operacao deve ser armazenado.
""" 



