# Tipos de Variaveis

var_int = 12 # Numeros inteiros

var_float = 15.3 # Numeros com casa decimais (Reais)

var_string = 'Conjunto de caracteres alfanumericos' # Incluindo simbolos e caracteres especiais

var_lista = [1, 2, 'Paulo', 'Maria', True] # Lista de dados e/ ou valores (sempre entre colchetes)

var_dicionario = {'nome': 'Fernando', 'idade': 31} # Conjunto de {dados: valores}, indexada (sempre entre chaves)

var_tupla = ('Ana', 'Fernando', 3) # Lista de dados e/ ou valores imutaveis, indexada (sempre entre parenteses)

var_bool = True # Booleano: True/ False; 0 e 1

var_comentario = """ Um comentario atribuido a uma
                    variavel deixa de ser apenas
                    um comentario, vira um texto
                    que pode ser incorporado ao
                    codigo..."""

